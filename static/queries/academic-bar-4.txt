#defaultView:BarChart
SELECT ?anos (COUNT(DISTINCT ?item) AS ?Produção) ?legenda
WHERE {
  ?item wdt:P361 wd:Q18477654 ;
        wdt:P577 ?date .

  BIND(STR(YEAR(?date)) AS ?anos)

  #substitui o "All" pela palavra "NeuroMat"
  BIND("NeuroMat" AS ?legenda)
}
GROUP BY ?anos ?legenda
ORDER BY ?anos